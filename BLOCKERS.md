# Blocker'lar — Kamp.la

Mustafa'dan aksiyon bekleyen maddeler. Kaynak: `Kurulum-Gereksinimleri.md`
(detaylı adımlar orada). CEO ajanı bu hesapları kendisi açmaz.

| # | Madde | Neden Gerekli | Durum | Not |
|---|---|---|---|---|
| 1 | Supabase projesi (URL, anon key, service_role key, PostGIS enable) | Faz 0-1: veritabanı, auth | ✅ Tamamlandı | Anahtarları `.env`'e sen gireceksin (kod hard-code etmeyecek) |
| 2 | Google Cloud OAuth client | Faz 1: Google ile giriş | Bekliyor | Ertelenebilir — e-posta/şifre girişi bağımsız çalışır |
| 3 | MapTiler API key | Faz 2: harita tile (topografik/uydu katmanı) | ✅ Tamamlandı | Mustafa root `app/.env`'e ekledi; CEO ajanı Faz 1'deki aynı sorunu (Nuxt paket bazında `.env` okuyor, kökü okumuyor) fark edip `apps/mobile-web/.env`'e kopyaladı — `useMap.ts` gerçek key'i otomatik algılayıp MapTiler stillerine geçecek |
| 4 | Cloudflare R2 bucket + API token | Faz 4: fotoğraf yükleme | Bekliyor | Geçici olarak Supabase Storage ile başlanabilir |
| 5 | Apple Developer Program + Google Play Console | Faz 9-10: Premium/mağaza paketleme | Bekliyor | Şimdilik gerekmez |
| 6 | RevenueCat hesabı | Faz 9: Premium ödeme altyapısı | Bekliyor | Şimdilik gerekmez |
| 7 | Faz 1 DoD manuel testi: kendi makinende `pnpm dev` ile kayıt ol + giriş yap + şifremi unuttum dene | Auth kodu sandbox'ta gerçek Supabase anahtarlarıyla test edilemiyor (sadece typecheck yapıldı) | ✅ Tamamlandı | `.env` dosyası monorepo kökündeydi, `apps/mobile-web/`+`apps/admin/` içine kopyalandıktan sonra çalıştı |
| 8 | Faz 2 DoD manuel testi: kendi makinende `pnpm dev` ile Ana Ekran'da harita/katman modalı/pin-özet kart/konumuma git, `/liste` (favori kalp, harita toggle), `/ara` (öneriye tıklayınca haritanın odaklanması), filtre modalı (2 kategori sınırı uyarısı) dene | Sandbox'ta typecheck + `nuxt dev` HTTP 200 doğrulandı ama gerçek tarayıcıda tıklama/görsel akış hiç test edilmedi | Bekliyor | MapTiler key olmadığı için harita OSM/demo tile ile placeholder görünecek — bu beklenen, key BLOCKERS #3'te ayrı bekliyor |
| 9 | Faz 3 DoD manuel testi: kendi makinende `pnpm dev` ile `/konum/[id]` sayfasını dene — foto galerisi (kaydırma), Detaylar/Yorumlar sekme geçişi, amenity tooltip'leri, koordinat kopyalama, mini harita, "Yorum yazın" modalı, alt aksiyon çubuğu (5 ikon) ve Yol Tarifi seçim modalı | Sandbox'ta typecheck + `nuxt dev`/`nuxt build` ile 7 farklı URL (mock POI'ler + not-found durumu) HTTP 200 doğrulandı ama gerçek tarayıcıda görsel/etkileşim hiç test edilmedi | Bekliyor | Ana Ekran'daki pin'e tıklayıp `/konum/mock-loc-001` gibi bir mock id'ye gitmeyi dene; misafir modunda alt aksiyon butonlarına tıklayınca `/giris`'e yönlenmeli |

| 10 | Faz 4 DoD manuel testi: yeni migration'ı (`supabase/migrations/0010_location_photos.sql`) kendi Supabase projene uygula (`supabase db push` ya da dashboard SQL editor), `location-photos` Storage bucket'ının oluştuğunu doğrula, `pnpm dev` ile `/konum-ekle/1`'den başlayıp 6 adımı uçtan uca tamamla (foto yükleme dahil), `locations` tablosunda yeni satırın `status='pending'` olarak göründüğünü kontrol et | Sandbox'ta typecheck + `nuxt dev`/`nuxt build` doğrulandı ama gerçek Supabase bağlantısı/insert/storage upload bu ortamda hiç test edilemedi | Bekliyor | Migration'ı uygulamadan sihirbazın 6. adımı hata verecektir (kolon yok); foto silme yalnızca formdan kaldırıyor, storage'daki dosya kalıyor (bilinen küçük eksik, orphan temizliği ileride) |

| 11 | Mimari karar: mock POI vs. gerçek `locations` entegrasyonu — hangi seçenek (gerçek entegrasyon / hibrit / erteleme), bkz. DECISIONS.md 2026-07-26 | Faz 5 (yorum/favori/liste-içeriği) mock POI id'leriyle DB seviyesinde çalışmıyor (FK uyuşmazlığı) | Bekliyor | Hesap/ödeme değil ama "geri dönüşü zor" bir mimari karar — CEO ajanı otonom karar vermedi, 3 seçenek sunuldu, bir sonraki daily'de görüşülmeli |
| 12 | Faz 6 DoD manuel testi: kendi makinende `pnpm dev` ile `/hesabim` (profil bilgisi, "Profili Paylaş", "ziyaret ettiğim yerleri göster" toggle, "Hesabımı kalıcı sil"), `/hesabim/kullanici-adi`, `/hesabim/e-posta` (gerçek Supabase e-posta değişikliği onay maili akışı dahil), `/hesabim/sifre`, `/hesabim/sosyal-medya`, `/ayarlar/hakkinda`, `/kullanim-kosullari`, `/gizlilik` sayfalarını dene | Sandbox'ta typecheck + `nuxt build` (production) doğrulandı ama gerçek Supabase bağlantısıyla (özellikle e-posta değişikliği onay maili, kullanıcı adı unique constraint hatası, hesap silme server route'u) hiç runtime test edilmedi | Bekliyor | Hesabı kalıcı silme `server/api/hesap-sil.post.ts` üzerinden `service_role` key gerektiriyor — sandbox'ta bu key yok, gerçek silme denemesi dikkatli yapılmalı (geri dönüşü yok) |

| 13 | Figma gerçek ikon/görsel export'ları: Ana Ekran/Katman Seçim/POI özet kart/Liste/Filtre/Arama ekranlarındaki ikonlar şu an Figma screenshot'larına bakılarak elle yeniden çizilmiş SVG'ler (gerçek Figma export'u DEĞİL) | `download_assets` çağrıları Figma "Starter plan" tool-call limitine takıldı + sandbox ağ proxy'si figma.com asset CDN'ini engelliyor (`blocked-by-allowlist`), bu yüzden gerçek export baytları hiç indirilemedi | Bekliyor | **Güncelleme (2026-07-26, Faz 3 uyum denemesi):** Kota sorunu sanılandan daha geniş — bu oturumda `get_design_context`/`get_metadata`/`get_screenshot` (okuma amaçlı araçlar) da 3 ayrı denemede (aralara 30-40sn bekleme koyarak) hep "Starter plan tool call limit" hatası verdi. Yani artık sadece ikon indirme değil, HİÇBİR Figma okuma işlemi çalışmıyor. Node ID listesi STATUS.md/FIGMA.md'de. Mustafa'nın kendi makinesinde (gerçek internet erişimi olan bir ortamda) Figma MCP ile `download_assets`/`get_design_context` çalıştırıp hem placeholder SVG'leri hem de henüz Figma ile karşılaştırılmamış Faz 3 (POI Detay) layout/spacing detaylarını gerçek değerlerle güncellemesi gerekiyor — ya da Figma planı yükseltilirse/kota sıfırlanırsa CEO ajanı bir sonraki oturumda tekrar dener |

**Not:** Faz 0'da hesap eksikliği kodu bloklamıyor — Supabase zaten hazır
olduğu için gerçek migration'lar uygulanabilir; MapTiler/R2/OAuth
gerektiren kısımlar placeholder/mock ile ilerler, gerçek anahtar
girildiğinde çalışır hale gelir.
