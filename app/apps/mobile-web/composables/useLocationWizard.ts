/**
 * Placeholder — Konum Ekle 6 adımlı sihirbaz state yönetimi Faz 4'te
 * burada kurulacak (PRD 5.G). Şimdilik `stores/locationWizard.ts` Pinia
 * store'una ince bir composable sarmalayıcı.
 */
import { useLocationWizardStore } from "~/stores/locationWizard";

export function useLocationWizard() {
  return useLocationWizardStore();
}
